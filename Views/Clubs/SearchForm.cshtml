@model WebApplication1.Models.ClubsModel

@{
    ViewData["Title"] = "SearchForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>SearchForm</h1>

<h4>ClubsModel</h4>
<hr />

@using (Html.BeginForm("SearchForName", "Clubs"))
{

    <div class="row">
        <div class="col-md-4">
            <form asp-action="SearchForm">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">

                    <label for="searchPhrase">Search By Name</label>
                    <input type="text" name="searchPhrase" id="searchPhrase" class="form-control" />

                </div>

                <div class="form-group">
                    <input type="submit" value="Search" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
}


@using (Html.BeginForm("SearchForDescription", "Clubs"))
{

    <div class="row">
        <div class="col-md-4">
            <form asp-action="SearchForm">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">

                    <label for="searchPhrase">Search By Description</label>
                    <input type="text" name="searchPhrase" id="searchPhrase" class="form-control" />

                </div>

                <div class="form-group">
                    <input type="submit" value="Search" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
}
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
